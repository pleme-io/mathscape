apiVersion: v2
name: mathscape-web
description: Mathscape frontend — React dashboard served by Hanabi BFF
type: application
version: 0.1.0
appVersion: "0.1.0"

dependencies:
  - name: pleme-lib
    version: "~0.4.0"
    repository: "oci://ghcr.io/pleme-io/charts"
